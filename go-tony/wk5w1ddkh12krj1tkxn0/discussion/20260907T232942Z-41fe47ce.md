<!-- 2026-09-08T01:29:42+02:00 -->

-m Revised (b0aeb5a): the stored form of a keyed array is an OBJECT whose field names are the element names, tagged to present as an array. Same construction the format already has for sparse arrays.

That dissolves the streaming fork instead of deciding it -- the name IS the field name, so it precedes the content by construction. No lowering order rule, no pending state. The substrate needs no schema: CurrentPath answers a field path, snap indexes fields, the trie keys on segments, and the keyed merge below the line is an object merge. Two of thqtmm2th12kr051jhn0's three defects stop having a subject.

Costs taken: keyed arrays read back in name order (the sorted contract already in force), and the key appears in both the name and the element, name authoritative.

Left open: whether the stored element segment keeps a distinct kpath kind rendered from the container tag -- that teaches the format library the tag, so the field form is taken here.
