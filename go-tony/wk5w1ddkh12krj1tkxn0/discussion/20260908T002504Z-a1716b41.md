<!-- 2026-09-08T02:25:04+02:00 -->

-m Prerequisite 2 drafted: go-tony/system/logd/storage/docs/presence.md (493ad35). Narrowed from what the issue asks, and the narrowing is the finding.

(nil, nil) is fine and stays. It is already the idiom, documented as deliberate in mergeop, and a type there enforces the part that never failed. What failed is that the rule lived in three files as three comments.

The collapse is one function. subtreeOf (session.go:379) returns ir.Null() three times for two different facts. api.SameState answers false for nil against null, so the watch gate would have told the truth -- its inputs were flattened one call earlier. The null-then-deleted path delivers no event because of those three lines, not the comparison.

The rule, once, beside NextState and SameState: a nil *ir.Node is ABSENT, null is ir.Null(), no layer may map one onto the other.

A type earns its place at two boundaries, neither a Go return. The wire, which cannot say absent at all -- sendInitialState sends null and watchAbsence fills the gap in the LOG ("a null subtree is not an arrival: it is how an absent path is delivered"). And the third state, not-determined, which is the only one that ever returned a wrong ANSWER (c3e53a2): the rule there is that a read either determines or errors, and prerequisite 1 closes it by removing the narrowing that could decline.

Also: agreed on the 30.1%. Those segments are ~100% spine and get skipped, so it is index size, not read cost -- same shape as the 1,386.
