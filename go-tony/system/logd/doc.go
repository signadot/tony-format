// Package logd provides the backend storage for the tony system api.
//
// Package logd implements a diff based virtual document store with
// transactional support sufficient for the system api.
package logd
