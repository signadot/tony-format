<!-- 2026-08-17T03:48:10+02:00 -->

Removed rather than repaired, in 5a4e1ec. Nothing used it -- not the CLI, not docd, not verse, nothing in any repo here, only the two tests written for it -- and repairing it meant giving it the whole checking apparatus a write goes through plus a read against the PENDING index, since baseline is the wrong base for any migration patch but the first. Migration itself is unchanged: propose a schema, dual-index ordinary writes while it runs, complete or abort. The transformation half is gone, and the way to transform data during a migration is the one the schema docs already recommend for auto-id changes -- ordinary patches, which are checked.
