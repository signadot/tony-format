<!-- Comment 001 - 2026-07-20T17:11:54+02:00 -->

Scope-snapshot CREATION removed in commit 02a3239 (SwitchDLog loop + CreateScopeSnapshot API + 2 tests). Remaining dormant scope-snapshot code to excise as part of this issue's work (all now unreachable for creation, since all createSchemaSnapshot callers pass nil scope):
- createSnapshot / findSnapshotBaseReader scope params + scope branches
- activeScopes tracking (field/mutex, trackScope, untrackScope, getActiveScopes, getAndClearActiveScopes) and its call sites
- compaction buildSnapshotGroups scope-snapshot filtering (activeScopes param)
These are replaced by the bounded, sound scope-overlay compaction this issue introduces.
