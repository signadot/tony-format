<!-- Comment 001 - 2026-07-24T09:45:20+02:00 -->

Fixed in b23fdee. Added Storage.commitMu held across GetCurrentCommit -> evaluateMatches -> NextCommit -> WriteAndIndex so the compare and swap are atomic across transactions. Fan-out notify moved out of WriteAndIndex to CommitOps.Notify, called after the lock releases, so this does NOT serialize commits behind a slow watcher (the fan-out-on-commit-path issue stays separate). Regression test cas_test.go (N concurrent PatchIf, same precondition -> exactly one commits) fails before / passes after, including -race. Full ./system/... suite green.
