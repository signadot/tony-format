<!-- Comment 001 - 2026-07-26T16:05:48+02:00 -->

Verified fixed. Reproduced the exact shape — a type with an `optional` nested
struct field:

    //tony:schemagen=cfg,notag
    type Cfg struct {
        Gateway GatewayConfig2 `tony:"field=gateway,optional"`
    }

Against current go-tony, codegen now emits the `isZeroValue_Cfg_Gateway` helper
(and imports `reflect`), and the package compiles. This was fixed as part of
f69agjyeh12ks item 12 (codegen: honor omitzero on a value-struct field, b8d46fa,
released in go-tony v0.0.90): the same emission guard that wraps a value-struct
field in an `isZeroValue` check also generates the helper for optional nested
struct fields.

Closing as fixed in v0.0.90.
