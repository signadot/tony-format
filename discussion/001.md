<!-- Comment 001 - 2026-07-28T08:15:18+02:00 -->

Verified against main at `911d959`. The bug is unchanged: the root read starts from the
snapshot, every read below it still replays from commit 0.

```
snapshot created commit=21, reading at commit 24
kp=""                   startCommit=22   uses snapshot
kp="demo"               startCommit=0    REPLAYS FROM 0
kp="demo.x.hot"         startCommit=0    REPLAYS FROM 0
kp="other.y.untouched"  startCommit=0    REPLAYS FROM 0
```

`findSnapshotBaseReader` still takes `kp` and opens with `s.index.IterAtPath(kp)`
(`snap_storage.go:23`), and `storage.go:136` / `:169` still pass the read's path.


- `67220e2` (07-27 01:37) **fixed it** — dropped the `kp` parameter, looked the snapshot up
  at the root, took the patch range at the root too, and added `TestSnapshotBoundsReplay`
  asserting on `startCommit` rather than latency.
- `20c48aa` (07-27 02:14) **reverted it**, 37 minutes later, with no reason recorded beyond
  "This reverts commit 67220e2".
- `d18e8cc` (07-27 02:50) then landed *"logd: differential harness for the snapshot read
  path (step 0, lands red)"*, followed by `3712a71`, `e224796`, `add58ce`, `215344f` and the
  `worktree-logd-read-path` merge at `979fdc6`.

So the revert reads as deliberate sequencing — back the fix out, build the differential
harness first, then redo the read path properly. That harness and the read-path work landed.
**The snapshot lookup itself never came back.**


Cherry-picked `67220e2` onto current main and ran the full suite: `go test ./...` clean,
including `system/logd/storage`. Two trivial conflicts from code that landed after it, both
in its test file and neither behavioural:

- `nodeAt` is now also declared in `read_snapshot_cases_test.go` (added later) — duplicate
  helper
- the `ir` import becomes unused once that helper is dropped

That is not a claim the fix is right — the differential harness exists precisely because
this read path is subtle, and whoever picks this up should run `67220e2` against the harness
that came after it rather than trusting a green suite. But it does mean there is no known
blocker on record, and the next person should start from that commit instead of writing it
again.

**Worth recovering the actual revert reason if anyone remembers it.** A bare revert with a
merged, passing fix and no recorded objection is the one piece of context that would change
how this is approached.
