<!-- Comment 001 - 2026-07-20T04:11:35+02:00 -->

Implemented as of v0.0.76. The architecture is in place: async multiplexed
LogdSession with read-pump, id-correlation, watch, COW scope, and reconnect
(f0715ea, 674af34, ced8522); the controller-side runtime — RunController +
Handler (Match/Patch/Watch), post-mount op loop, unsupported-error decline —
which fills the DocdSession/Coordinator role (M1b, 3594400); docd->controller
op routing with id multiplexing; and deterministic "outcome unknown" to the
client on controller crash (failAllRoutes). Watch replay across a backend drop is
handled client-side by LogdSession (the switchability dividend), so docd stays a
thin proxy rather than duplicating it (see umbrella comment on dial-backoff).

Closing; LogdSession/PatchWith gained NewTx/PatchTx/PatchIf and the tx/CAS paths
on top (a926d8b, 0ac7428).
