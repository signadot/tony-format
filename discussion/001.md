<!-- Comment 001 - 2026-07-20T15:16:41+02:00 -->

Landed on main as go-tony/v0.0.78.

Done (tested, green under -race):
- COW scope conveyed across the mount boundary (additive SessionRequest.Scope;
  controllers honor it, reference logdController keeps per-scope sessions) — ecde366.
- scope-aware composed reads; scoped multi-mount tx (tx created in-scope via
  allocScopedTx, base participant + routed sub-patches scoped) — 7e151d0.
- e2e: ScopedSingleMount, ScopedComposedMatch, ScopedMultiMountTransaction.

Dogfood consumer confirms scopes work for their case (no nested mounts, no
cross-scope writes).

Follow-ups (not blocking; tracked):
- scoped composed WATCH is threaded but lacks an e2e test (needs a scope-aware
  watchable reference controller).
- logd's own COW isolation is buggy (a later baseline write overrides a scope's
  prior write to the same path) — issue eagjggjdh12ksg00bsn0. Avoided as long as
  no path is written from more than one scope.

Closing: the scoped multi-mount transaction work is complete and shipped.
