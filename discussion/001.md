<!-- Comment 001 - 2026-07-28T08:35:48+02:00 -->

Fix `94ad8bc` (token: carry a multi-byte rune across a buffer refill) is merged.

Re-ran this issue's own reproduction — the `roundTrip` helper over
`strings.Repeat("em-dash — arrow → check ✓\n", n)`, which failed at 8000 body bytes when
this was filed:

```
body   7680 bytes: ok      body  12800 bytes: ok
body   9600 bytes: ok      body  32000 bytes: ok
body  10240 bytes: ok      body 128000 bytes: ok
```

Clean at every size through 128 KB, well past the 8 KiB refill boundary.

Note the issue's second ask — "report the offset of the offending byte rather than the token
start" — was not separately verified here. Reopen or file a follow-up if that is still wrong;
it is a diagnostic-quality item, not the session-killing bug this issue is named for.
