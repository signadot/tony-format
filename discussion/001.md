<!-- Comment 001 - 2026-07-24T10:12:52+02:00 -->

Fixed in 753e244. Generation now persisted durably in dlog.state (fsync) and restored on open; interrupted compactions recovered from .old/.compact.tmp on startup; directory fsync'd after the swap renames; and a generation-mismatch on open triggers an index rebuild from the logs as a safety net. Three regression tests (clean reopen / crash-between-renames / crash-after-swap-before-index-persist) fail before and pass after, incl -race. Full ./system/... green. Note: a clean Close+reopen after compaction already reproduced the read failure — not crash-only.
