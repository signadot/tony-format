<!-- Comment 001 - 2026-07-27T20:53:10+02:00 -->

-b The `!key` half is fixed on branch issue-7f8rsk22, since it was one nil and one
dropped tag rather than a question about coordinates:

- `keyedListOp.Patch` wrote `pf`'s result into `dst[i]` unconditionally, and
  that result is nil when the item's patch removed it (`!delete`). The nil then
  reached `ir.FromSlice`. It now leaves the list instead of becoming a hole in
  it.
- the same function returned `ir.FromSlice(res)` untagged, so patching a keyed
  list stopped it being one -- the `!key(name)` was dropped. It now keeps the
  document's tag.

`diffTests[9]` and `[10]` round-trip. What remains open here is `diffTests[2]`,
the arraydiff index numbering, which `TestDiffTestsRoundTrip` skips by name.
