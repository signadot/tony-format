<!-- Comment 001 - 2025-12-20T11:40:08+01:00 -->

Claiming this issue. The test expects JSONPath format (`$.items`) but CurrentPath() returns kinded paths (`items`). Fix: update test to check for `items` prefix instead of `$.items`.
