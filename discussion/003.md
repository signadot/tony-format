<!-- Comment 003 - 2026-07-28T08:35:49+02:00 -->

Fix `a5ad734` (token: terminate scans when the reader is drained) is merged.

Dropped the attached `streaming_boundary_bugs_test.go` into `token/` unmodified and ran it
against main at `911d959`:

```
--- PASS: TestStreamingBoundaryBug_Tag
--- PASS: TestStreamingBoundaryBug_BlockString
--- PASS: TestStreamingBoundaryBug_MLitAtEndOfDocument
```

All three bugs — the silently truncated tag (the severe one, silent corruption at four of
five boundary positions), the block-style quoted string in a document larger than the
buffer, and the end-of-document multiline literal — pass.
