<!-- Comment 003 - 2026-07-20T20:58:12+02:00 -->

#2 done (commit 3c874c4): docd tracks the highest commit delivered per watch path and stamps it on the terminal WatchEvent; libctl surfaces it as WatchEndedError.Commit so the app can re-watch FromCommit. Exact for single-route watches; test covers the controller-crash case (differential-verified).

Toward '#1 as a real bug': confirmed by code inspection that startComposedWatch IGNORES req.Watch.FromCommit entirely — a composed (re-)watch always sends the current composed snapshot (composeReadTree) and goes live with NoInit sub-watches; there is no replay path. So a composed re-watch cannot replay the end->re-watch gap even given the resume commit from #2. That's the concrete bug behind 're-watch FromCommit did not recover' — NOT the absence of graceful membership update. Open: whether live delivery also breaks after a composed re-watch (the repro's 'permanent no events') or only the gap is lost; to be settled empirically next.
