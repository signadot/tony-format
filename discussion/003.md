<!-- Comment 003 - 2026-07-28T08:15:18+02:00 -->

Both corpus cases now apply. `TestDiffTestsRoundTrip` on main at `911d959` runs all 14 cases
with **zero skips** and passes:

```
--- PASS: TestDiffTestsRoundTrip/0 .. /13   (14/14, no Skip calls in the run)
```

Cases 2 and 9 — the two this issue named, which the test used to skip while naming this
issue — are among them.

Fixed by:

- `eed3486` diff: an arraydiff key is a position, and Reverse may not walk into values — the
  index-numbering half (`DiffArrayByIndex` folding a delete+insert into a `!replace` while
  still consuming a slot, so every later key was one too high)
- `98d994a` diff: emit `!raw` for operators held as data, and make `Patch(a, Diff(a,b)) == b`
- `3dcb44f` libdiff: delete PatchArrayByIndex, the copy nothing called — removes the drifted
  duplicate this issue flagged, so there is no longer a guarded copy and an unguarded live
  one to diverge

The invariant this issue argued for — `Patch(a, Diff(a, b)) == b`, held over the corpus
rather than only checking diff text and `Reverse(Reverse(d)) == d` — is now enforced by
`diff_roundtrip_test.go` with nothing excluded.

Reopen if a case is found that the corpus does not cover; the original concern that
`TestDiff` alone could pass an inapplicable diff remains valid as a general risk, it is just
no longer unguarded.
