<!-- Comment 003 - 2026-07-28T08:35:46+02:00 -->

Fix `1d2538e` (kpath: add wildcard path matching — Matches/MatchesPrefix, HasWild) is merged.

This issue's own table, run against main:

```
pattern                target                     Matches  MatchesPrefix
review.seq[*]          review.seq[2]              true     true
review.seq[*].onDone   review.seq[2].onDone       true     true
review.*               review.onDone              true     true
review.seq[2]          review.seq[2].onDone       false    true    (concrete prefix)
review                 review.seq[2].onDone       false    true    (concrete prefix)
```

Every wildcard row that was `false false` is now matched, and the concrete-prefix rows keep
the correct ancestor-not-equal behaviour. `HasWild()` is added and `Wild()` is documented as
head-segment-only, which closes the silent trap in ask 2.

Ask 3 (a kind-spanning "any element" wildcard, matching dense/keyed/sparse alike) was
answered rather than implemented: matching is deliberately kind-strict, and a `(*)` syntax
would be a separate addition. File that separately if it is still wanted.
