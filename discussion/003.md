<!-- Comment 003 - 2026-07-20T04:11:35+02:00 -->

Complete as of v0.0.76. The initial-scope deliverables are all shipped: TCP mount
listener + MOUNT handshake (46d3f61, eeaeca8), mount registry with prefix routing,
PATCH/MATCH/WATCH routing to controllers, and `o sys up|down` composing logd+docd
(o sys docd serve, o sys up). The first-comment ask (o sys docd, o sys up
composing docd+logd, docd caching tx's from logd) is done — the tx pool is wired
(a926d8b, backfill 7e8b1b7).

Note: the routing model evolved from this issue's sketch (reads went "direct to
logd") to reads-through-the-owning-controller, per the docd umbrella
(wcabztj2h12ksb9qbnn0) design decisions. Closing; the broader library work
continues under the umbrella.
