<!-- Comment 003 - 2026-07-28T08:35:46+02:00 -->

Both halves are merged:

- `ad0e201` mergeop: `!raw`, the escape that lets a patch carry an operator tag as data —
  `RawTag = "!raw"` (`libdiff/constants.go:10`)
- `98d994a` diff: emit `!raw` for operators held as data, and make `Patch(a, Diff(a,b)) == b`

The round-trip harness this issue's investigation produced now runs the whole corpus with
nothing excluded — `TestDiffTestsRoundTrip` passes 14/14 with zero `Skip` calls on main at
`911d959`. The two corpus cases it originally had to skip were split out to
`7rdzbjjbh12krw58cxn0`, which is also fixed and closed.
