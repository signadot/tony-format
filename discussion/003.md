<!-- Comment 003 - 2026-07-28T08:57:39+02:00 -->

`67220e2` is back, unchanged in substance. The two conflicts on reapply were both in its
own test file and neither behavioural: `nodeAt` is now also declared in
`read_snapshot_cases_test.go` (added after the revert), and the `ir` import goes unused once
that duplicate is dropped.


`20c48aa` recorded no reason. What followed reads as sequencing rather than rejection:

```
01:37  67220e2  the fix
02:14  20c48aa  revert, no reason given
02:50  d18e8cc  differential harness for the snapshot read path (step 0, lands red)
03:09  3712a71  apply patch roots the base does not reach, and fold dominated ones
03:34  e224796  graft unreached patch roots in sorted position
03:39  add58ce  write down the key-order contract for storage
04:04  215344f  write an absent subtree by writing nothing (class 3)
04:09  979fdc6  merge worktree-logd-read-path
```

Back the fix out, build the oracle, fix the substrate the oracle finds wanting, put the fix
back. Every step but the last happened.


`TestReadEquivalence_SnapshotVsReference` runs two storages over an identical write stream:
A never snapshots, so every read takes `ApplyPatches`' empty-base branch; B snapshots, so
its reads go through `findSnapshotBaseReader`. Any divergence is a read-path bug.

While the lookup was broken, B's below-root reads *also* fell to the empty base — so for
exactly the reads this fixes, A and B agreed vacuously. The harness has been passing on a
path it was not exercising. With the lookup working it exercises it.

```
TestReadEquivalence_SnapshotVsReference    LOGD_SEEDS=500    PASS (163s)
TestSnapshotRead_NewSubtreeSurvives                          PASS
TestSnapshotRead_AncestorWriteKeepsDescendantWrites          PASS
TestSnapshotRead_AncestorDelete                              PASS
TestSnapshotRead_RootDelete                                  PASS
```

And the bug itself, snapshot at commit 21, reading at 24:

```
before:  kp="demo.x.hot"  startCommit=0    replays from 0
after:   findSnapshotBaseReader(24) -> startCommit=22
```

Full repo `go test ./...` green; `go test -race ./system/logd/storage/...` green.

The 500-seed soak is the evidence I would lean on if the revert turns out to have been
deliberate — it is the oracle built specifically to judge this change, run 20x its default.
If a reason surfaces that the soak would not have caught, reopen.
