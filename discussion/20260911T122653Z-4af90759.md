<!-- 2026-09-11T14:26:53+02:00 -->

On main, 7db9376..610de0a: every item is fixed, dropped (no callers in go-tony or verse), or has its comment/doc corrected, except:

- design questions, left: base-type refs without MergeBaseDefinitions; schemasByURI never written; lowering keys that differ only in type, and the !raw escape.
- generated code, left: DirOutput.Filenames, api.Error.
- not a defect: Build's lastFile. The walk reads ahead only in the log it just returned, so lastFile names the failing log.
- left: storage/CLAUDE.md; gomap's three planning .md files.
- kept: OpContext.DefEnv/EvalOpts. verse sets them; nothing reads them.
