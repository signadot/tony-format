<!-- 2026-08-27T09:47:03+02:00 -->

Renamed after the fact: !get-paths is !list-path (ae61227). Everything above which
says get-paths means list-path.

The pair is two walks -- ir.Node.GetKPath answers a node, ListKPath answers the nodes --
and get-paths pluralised the wrong word: what is plural is the answer, not the path, and
there is exactly one path in both. Behaviour is unchanged.
