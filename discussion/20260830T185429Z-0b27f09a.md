<!-- 2026-08-30T20:54:29+02:00 -->

-b Done in 9e10b43.

WatchEvent grows `endMessage` (regenerated via `make generate`, and generate-verify is
clean). failWatch passes the message it was already composing, so replay_compacted now
reaches the client with the exact-from commit in it.

docd was dropping the same thing one layer up, which I had not seen when filing: a
sub-watch failure arrives as an error with a code AND a message, and `forward()` took only
the code. A composed client cannot see the mount that failed, so the code alone told it
nothing actionable. `terminateWatchWith` relays it, and the composed watch's own
match_failed now carries the read error behind it.

libctl surfaces it as `WatchEndedError.Message`, and `Error()` includes it.

The reason code is untouched and remains the thing to switch on -- a fixed vocabulary,
with the prose beside it rather than folded into it.

TestWatchEnded_ClientLearnsTheWatchDied asserts the message names the commit the store can
still serve from. Verified by dropping the message again: it fails on that line.
