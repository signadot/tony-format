<!-- 2026-09-08T12:06:09+02:00 -->

-m Answered by option 2, built as phase 5 of the logd rebuild (wk5w1ddkh12krj1tkxn0): de41b0f. Every watch event is rooted at the watched path. A baseline watch projects the stored delta onto its path with the read's own projection (api.ProjectDelta) rather than rewriting the committed delta -- the concern that rewrapping loses operator fidelity no longer applies, since a stored delta is absolute (phase 3b) and a projection is a subtree of it. docd re-roots a mount's delta to the composed path with the same function. Session protocol 2; a client at 1 is refused at the handshake. The client's side is stated in libctl's package doc.
