<!-- 2026-08-15T02:04:14+02:00 -->

STILL FAILS on go-tony v0.0.134, with a byte-identical error, after restarting docd so the store process is certainly running the new build:

    failed to apply patches: let patching "null" gave cannot patch with let operation

And it is NOT the charter path. The smallest form is one ordinary entity:

    verse entity put demo:poison:1 '!raw {value: !let {let: [{tip: abc}], in: {base: !not .[tip]}}}'
    verse entity get git:head:verse      # → the error above, for EVERY entity
    verse entity rm demo:poison:1        # → reads recover immediately

THE CONTRAST, same binary on both ends (verse 441d3aa2, go-tony v0.0.134):

| payload | staging | fresh local verse |
|---|---|---|
| `!raw {value: !let …}` | poisons every read | fine |
| `!raw {value: !glob …}` | fine (live in a rule for days) | fine |
| `!raw {value: !irtype …}` | not tried on staging | fine |

So the code is the same, the payload is the same, and one store holds it while
the other cannot. The remaining variable is that store's STATE: staging's data
dir has ~1900 revisions and was created by older builds, where the local one is
seconds old. I tried to reproduce the age locally by pushing a fresh store past
1200 commits so logd snapshots -- no failure -- so it is not commit count alone.

What would isolate it, and only you can produce it: a copy of that data dir, or
whatever logd logs on the read that fails. Happy to run anything specific
against staging with a canary -- the outage is now seconds rather than minutes,
since the install script reads an entity immediately and removes the offending
write on the first failure.
