<!-- Comment 014 - 2026-07-20T01:56:21+02:00 -->

## txpool backfill landed + tx follow-ups split out

Backfill (7e8b1b7): the pool now refills in the background as it drains, so Gets
stay served from cache (amortized logd round trips) — the actual point of pooling.
Self-corrects to the participant counts actually requested. Race-clean test.

Transaction follow-ups spun into their own issues:
- qf58mz22h12krzk2bnn0 (BLOCKS this umbrella): docd-coordinated patch splitting —
  the *intended* transaction model. A client just sends a patch under conditions;
  docd splits it across the mounts it touches and commits atomically. The current
  explicit NewTx/PatchTx path is groundwork. Participant count = controllers + 1
  (the +1 is docd's own logd link).
- 3vm6gq21h12krv32bnn0: scoped multi-mount transactions (controllers are baseline
  today, so scoped multi-mount txns can't complete yet).
