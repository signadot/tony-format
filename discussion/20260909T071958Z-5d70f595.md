<!-- 2026-09-09T09:19:58+02:00 -->

-m Done by the logd rebuild (wk5w1ddkh12krj1tkxn0), phase 2 (12a92b8): a keyed array is stored as an OBJECT OF NAMES, so an element is a field and has a path of its own -- items."(sku=A)" -- and an operation written there is confined to it. The schema is the authority for what keys an array (!logd-key / !logd-auto-id), the write path lowers a client's array into that form and the read boundary raises it back, so array-ness stays the client's vocabulary and identity is the store's.
