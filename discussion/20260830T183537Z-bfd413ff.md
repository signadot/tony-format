<!-- 2026-08-30T20:35:37+02:00 -->

-b Fixed in 25a9e17.

EachPatchInRange walks the range one entry at a time; replay pushes each delta as it is
read. The dedup and the ordering moved into SEGMENT space as you suggested -- and the
final sort turned out to be sorting an already-sorted list, since a patch segment's
StartCommit is its commit less one, so LookupRange's order is already commit order.
ReadPatchesInRange stays, collecting from the walk, for callers whose range is small.

MEASURED as live heap at the moment of peak, 4000 commits of your payload shape: the walk
holds 1.1 MB at its last entry, the collector 45.0 MB.

A note on the instrument, since your harness measures the round trip: I could not make
that shape assert anything. Peak heap over a full client-server replay is dominated by
concurrent snapshotting, and the same streamed build measured +53 MB then +194 MB at 8000
commits across two runs -- 1.4x and 3.4x growth for the same code. The committed test
measures the storage call alone, with the collector beside it as a control that fails the
test if IT is not visibly holding the range, so a broken measurement cannot read as a
pass. Your harness remains the right thing for the end-to-end number.

One behaviour changes: a dlog read failing PART WAY has already emitted the earlier
patches. The floor check still precedes any delivery, so the compacted refusal is
unaffected, and the terminal event carries what was accounted for.

The smaller one beside it is real and wider than you framed it -- failWatch logs its
message and NewEndedEvent has nowhere to carry it, so every ending message in the watch
path is dropped, not only the compacted one. Filed as zmq8bdhwh12kstkqjhn0; it needs a
field on the wire.
