<!-- 2026-08-29T20:12:13+02:00 -->

Half of this is now false: a scoped read CAN be narrowed, which was the part of the baseline/scope gap that cost the most. narrowSubtreeAt takes a scopeID -- baseline's patches from the snapshot, then the scope's own over its whole history, both looked up at the read path via LookupSubtree and projected there. A scoped read at a path is now flat in the scope's history (~40us) where the wide read is linear (6.7ms at 400 scope writes).

Nothing is synthesized: these are the patches the scope wrote, replayed, so !key identity and every other operation mean at the path what they mean in the wide read. That is why it is sound where deriving a scope layer from two documents was not.

Still true, and still this issue: a scoped read cannot be STEPPED. NewScopedWatchStepper is gated on the overlay (scope_watch.go), and with the overlay now off by default a scoped live watch recomputes per event again.
