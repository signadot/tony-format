<!-- Comment 009 - 2026-07-28T08:35:50+02:00 -->

All seven commits from branch `issue-rkrvsf1c` are on main:

```
bfb774e  compose ancestor reads (MATCH) across mount boundaries
a74e9b5  compose ancestor watches across mount boundaries
4bd6d73  mount/unmount reader-writer coordinator (writer priority + force_after)
7ce13a1  coordinate mounts against overlapping watches (block + force_after)
142b5d8  signal watch termination so clients re-establish (no silent stop)
6a676df  per-mount force_after on the MOUNT protocol
6294729  graceful unmount (controller control-frame)
```

That covers both halves of the title — Match composition (step 1) and Watch composition
(step 3) — plus the step-2 mount/unmount coordination the design called for. Comment 007
records the work as "functionally COMPLETE ... Full system suite green under -race. Ready to
merge to main", and it has since merged.

Two things this issue spawned stay open on their own:
`favh47sxh12kraaebsn0` (mount coordination force-ends legitimate long-lived watches) and
`hb44wv28h12ksarmcdn0` (composed-watch ordering / stale replay).
