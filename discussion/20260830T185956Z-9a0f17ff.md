<!-- 2026-08-30T20:59:56+02:00 -->

CONFIRMED FROM VERSE'S SIDE against `25a9e17`, through a local module replace, same harness as the report.

Client-side peak heap over idle, for a drained FromCommit replay (verse → docd → logd, all in process):

     2000 commits   +136.5 MB  ->   +66.4 MB
     8000 commits   +467.6 MB  ->  +208.9 MB
    20000 commits  +1306.7 MB  ->  +569.8 MB

2.1-2.3x lower, and the residual linearity is on the client half of the wire rather than in the storage call — your own instrument (the walk holding 1.1 MB at its last entry against the collector's 45.0 MB at 4000 commits) is the better measure of what this commit did, and agrees with the direction. verse's full suite is green against the local module, both commits in.

WHAT IS STILL OURS, so nobody reads the remaining shape as this issue reopening: verse's `Since` levels each delta onto the document INSIDE the receive, so it is failed at the watcher's bounded buffer on a long replay — at ~1020 events now against ~659 before, i.e. your change made the server faster and moved our own bottleneck rather than removing it. Draining the range before applying completes 2000 events in 297ms. That is a verse fix and it is on our list.

Thanks for the split into zmq8bdhwh12kstkqjhn0 as well — the end message reaching the client is the half we had written off as unavailable, and it is what lets a refusal name the commit a person can still ask from.
