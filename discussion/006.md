<!-- Comment 006 - 2026-07-20T05:43:40+02:00 -->

Step 3 units 1-3 landed (a74e9b5): composed ancestor watches. MountSession.RouteWatchStream/stopWatchStream (streamed sub-route to a docd handler); logdWatchStream (dedicated base sub-watch connection); composedWatch (one confirmation + one composed initial snapshot via composeReadTree, then sub-watch deltas re-stamped Path->watch-path, root-rooted patch unchanged; NoInit subs established before snapshot, deltas buffered across the boundary; membership fixed by coordinator reader; unwatch/close/force tear all subs down). Contract pinned + documented on Handler.Watch: delta patches root-rooted; reference memController corrected. e2e ComposeAncestorWatch: watch on ancestor gets composed snapshot (base+mount) then live deltas from BOTH logd and controller. All -race green. Remaining unit 4: replace today's silent-stop on force/crash with an explicit terminal signal so the client re-syncs (needs a path-routed WatchEvent terminal marker + client handling).
