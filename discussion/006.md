<!-- Comment 006 - 2026-07-26T04:08:19+02:00 -->

Item 5 fixed — closing the issue; all items are addressed.

**Item 5** (nested calls drop `opts...`): two causes. `methodAcceptsOpts` compared the field type's `PkgPath` to the current package, but a type resolved from source is a placeholder `reflect.Type` with an empty `PkgPath` (`reflect.StructOf`), so it never matched a local type and reflection found no method on the synthetic type — now a struct placeholder with no package path is treated as a local generated type whose codecs accept opts. Separately, the value-struct FromIR branch hard-coded `FromTonyIR(fieldNode)` without the opts suffix (unlike the pointer branch); fixed. All 7 codegen packages with nested fields regenerate, no bare nested call remains, and the schemas and `o b` output are unchanged. (shipped unreleased on main; will ride the next tag)

Heads-up: verse's `codec/upstream_test.go` pins the current *bare* form and asserts nested calls do **not** pass opts — that test will flip red when verse updates past this. Expected, per the note at the end of item 5.

---

Resolution summary (go-tony):

| item | status |
|---|---|
| 1 dispatch ToTonyIR for non-pointer values | fixed, v0.0.88 |
| 2 named map dispatched to its codec | fixed, v0.0.88 |
| 3 diagnostic for an unresolvable type | fixed, v0.0.88 |
| 4 `codec=custom` (resolvable without generating) | fixed, v0.0.88 |
| 6 whitespace-only body is blank | fixed, v0.0.88 |
| 7 `map[string]any` supported | fixed, v0.0.88 |
| 8 named scalar resolves by underlying kind | fixed, v0.0.88 |
| (latent) bare `interface{}` field | fixed, v0.0.88 |
| 10A `omitzero` honored for all scalar kinds + reflection | fixed, v0.0.89 |
| 11 `omit` / `-` / `field=-` honored on reflection | fixed, v0.0.89 |
| 10B untagged empty **slice** emits `[]` | fixed, v0.0.89 |
| 5 nested calls pass `opts...` | fixed, on main |

One deliberate scope caveat: **10B is slices only.** Maps were left as-is because the reflection path distinguishes a nil map (`null`) from an empty map (`{}`) — a round-tripping unset-vs-empty distinction — which codegen cannot reproduce from `len` alone. If verse wants a generated type to put an explicitly-empty map on the wire, reopen (or file a follow-up) and I'll take the map case on with that semantics settled.

Every fix has a cross-path regression test; all 10 codegen packages regenerate cleanly; the logd/docd wire api changed only for `WatchEvent.EndReason` (10A, the documented omit), and the operator build (`o b`) is byte-identical throughout.
