<!-- Comment 006 - 2026-07-20T17:49:06+02:00 -->

Watch side fixed (commit 3c3227c): a scoped watcher now recomputes the scoped state at the watched path on each relevant commit and emits the diff vs the last emitted state. Baseline writes to scope-overridden leaves produce an empty diff and are suppressed; baseline writes elsewhere flow; !key stays durable. Applies to live + replay.

Also fixed a regression the read change introduced (769eab4): scoped reads are now computed in a single apply pass (baseline patches then scope patches) rather than materializing baseline and re-applying scope patches through a node<->events round-trip, which dropped scope writes to numeric-string field keys (e.g. path 'users.1'). Caught by TestDocd_ScopedSingleMount.

Full read + watch COW fix on branch logd-cow; logd/docd/libctl suites green. Remaining: bounded scope-overlay compaction (5hmq80f3).
