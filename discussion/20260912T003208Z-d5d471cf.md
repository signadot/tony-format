<!-- 2026-09-12T02:32:08+02:00 -->

Implemented on issue-cn1n32yp (91f9dd4d), as planned: author on hello/patch/event, protocol 3, one author per commit with tx_author_mismatch (which crosses the controller hop), docd resolves against the client's hello on every route it builds, libctl carries it in LogdSessionConfig.Author / PatchOpts.Author / PatchParams.Author. logd, docd, libctl suites green. Not pushed.
