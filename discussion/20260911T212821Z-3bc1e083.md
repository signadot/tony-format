<!-- 2026-09-11T23:28:21+02:00 -->

Fixed in 5140530. Every tx has a timeout: newtx's, else tx.timeout (default 1s -> 5m; 0 = default, never none). A participant without one waits the tx's. A closing session stops waiting unless the commit is running, so StopTCP is prompt. Still open: docd pooled ids die tx.timeout after creation, and a late participant can commit after docd's 10s participant timeout.
