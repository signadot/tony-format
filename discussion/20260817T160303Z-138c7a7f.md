<!-- 2026-08-17T18:03:03+02:00 -->

Fixed in 42b8231. Sweep is now part of the suite (token.TestEverySplitReadsLikeTheWholeDocument): every .tony in the tree, read sizes 1..4097, tokens compared against the whole document. 0 differences.

Not fixed, noted while here: Tokenizer.readStreaming (the tokenizer's own buffer, unused by TokenSource) compacts in place, which is the aliasing shape TokenSource.fillBuffer has a comment about avoiding. It hands out a fresh copy per Read, so no token aliases the compacted array today — it is a trap for the next caller, not a live bug.
