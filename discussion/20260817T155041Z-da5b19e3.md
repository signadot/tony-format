<!-- 2026-08-17T17:50:41+02:00 -->

Fixed in go-tony v0.0.158 (per Scott). verse is bumped to it in signadot/verse 95725549.

Verified from the verse side by driving what produced it, since it was never reproducible on demand: scoped charter add/remove churn interleaved with large values, and a sweep of string values from 4000 to 9000 characters in steps of 137 — across the column the error always named — each written and read back whole. Zero `bad literal` in any post-bump run (full suite several times, both smoke renderings, a live verse across several restarts). Before the bump, the same kind of driving produced three.
