<!-- 2026-09-08T01:55:10+02:00 -->

-m PROBE: snapshot construction does NOT materialize the document. It materializes every patch in the range.

The document side is intact and is what it says it is (snap_storage.go:164). findSnapshotBaseReader opens the previous snapshot and hands back ReadPathEventReader(""); patches.NewStreamingProcessor().ApplyPatches(baseReader, patchNodes, builder) flows events base -> builder -> log file. No node tree for the document at any point.

The patch side is the same defect the forensics measured on the read path, in a second place:

    segments := s.index.LookupRange("", &startCommit, &commit, nil)   // a slice
    for _, seg := range segments { ... patchNodes = append(patchNodes, entry.Patch) }
    snap.NewBuilder(snapWriter, snapIndex, patchNodes)

Every non-snapshot entry in the range decoded to *ir.Node and held at once before the streaming apply starts -- patchNodesFromSegments by another name, which is the 511 MB of the 743 MB read. Bounded by the log-switch interval rather than by history (measured store: 246 snapshots / 538,770 commits, ~2,190 per interval, last one 1,386 behind head), but not by the snapshot size, and it grows if snapshotting falls behind. Static read of the code, not measured.

Third: SwitchDLog calls CheckHead() right after, which is a full read by design -- "the one place a full read is already the order of the work being done". Sound today; under an API with no total read it needs a different form, and it is a caller prerequisite 1 has to answer for.
