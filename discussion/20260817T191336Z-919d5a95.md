<!-- 2026-08-17T21:13:36+02:00 -->

Cannot reproduce at the docd layer, in the configuration described (no mounts, no scope). Test added in 900e458: the three ids — plain, dotted, slashed — written and read back through both faces, baseline and scoped, then the object at demo.probe checked for one field per id.

    logd         demo.probe."probe.dotted"   committed, read back ok
    docd         demo.probe."probe.dotted"   committed, read back ok
    logd scoped  same
    docd scoped  same
    document:    probe-plain/k  probe.dotted/k  probe/slashed/k   (one field each, both faces)

With no mounts a client patch has one participant, so maybeCoordinatePatch declines and docd forwards the request verbatim. Every docd-side helper that touches a path keeps the segment whole: pathFields, fieldsToKPath, kpath.Field(...).String(), SplitAll + SegmentFieldName, nestAtFields.

So the diagnosis probably belongs upstream. The report's own detail is the lead I would follow: 'it also leaves a spurious entity behind at the truncated name' — that is two writes, one of them at a path rendered WITHOUT the quoting. If the store renders the path once for the entity write and again (differently) for an index, enumeration, or read-back, that second rendering is the suspect, not the transport.

One line settles it: log the exact Patch.Path string the store hands the session, for both faces, on the failing write. If docd receives `verse.demo.probe."probe.dotted"` and stores it nested, I want that case — it contradicts the test above and I will chase it. If it receives the unquoted form, the bug is in whatever produced it.

Leaving open: the symptom is real (192 refs left standing), only the layer is in question.
