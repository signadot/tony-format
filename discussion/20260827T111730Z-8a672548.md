<!-- 2026-08-27T13:17:30+02:00 -->

Reopening nothing, but recording where this sits, since it is closed and the thing it
interacts with is not.

The fix here -- a scope may not store a relative op -- is the SCOPE-SPECIFIC CONCESSION that
scope_overlay_plan.md rejected by name six days earlier (fdf4bdd): "A patch may be written with
arbitrary expressivity; what the log stores is Diff(before, after) ... the restriction belongs
on storage rather than being a scope-specific concession."

Both are in the tree and they point different ways. The closing note above offers this fix as
support for 5hmq80f3h12krh1mbsn0 -- "the premise is now ENFORCED rather than believed" -- which
is true, and is also why the conflict is easy to miss: it reads as a strengthening from that
issue's side and as a rejection from the plan's.

Nothing to undo here: this closed a real defect (a scope becoming permanently unreadable) with
the mechanism available at the time. What is open is whether it stays the answer or is
subsumed by the lowering, and that question now lives on 5hmq80f3h12krh1mbsn0, which is related
to this one as of today.
