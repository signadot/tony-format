<!-- 2026-08-17T11:01:56+02:00 -->

Index fix in c8afa60. Measured before: `accept: .[foo(int)]` with foo(t) satisfiable and plain foo not gave 71/200 accepted, 95 rejected for the wrong variant, 34 rejected at the accept build; the mirror (plain fine, param unsatisfiable) was ACCEPTED 56/200. After: 200/200 correct both ways.

Keying by full name alone was not enough — lookups arrive with a base name, so three sites needed arity: buildRef, findReachableDefinitions (its `visited` too), and tag references, which dropped their args. `visiting` is now keyed by definition, which is what lets array(t) refer to array without reading as an inescapable cycle.
