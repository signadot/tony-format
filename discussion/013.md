<!-- Comment 013 - 2026-07-26T16:35:23+02:00 -->

turned up a nastier codegen defect than item 18. FlowTree/FlowNode annotated cleanly. But entity.Entity cannot be described: codegen's generated code is right — it finds Ref.MarshalText and emits ir.FromString — while its generated signature says ref: .[int], because the schema type mapper never consults TextMarshaler. Nothing fails to compile and nothing misbehaves at runtime; only a published schema would lie, about the one identifier every surface shares. That's worse than an absent signature, so I backed the directive out and left the reasoning in the code. The field tags and MarshalText stay — they're correct and make the wire shape right when these become response types.

That's a third item for whoever has the tracking issue: schema output disagreeing with generated code is a distinct failure mode from items 3/8/14/18, all of which fail loudly at build.
