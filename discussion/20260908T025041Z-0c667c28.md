<!-- 2026-09-08T04:50:41+02:00 -->

WHERE THE BRANCH STANDS, end of the implementation session. All on issue-wk5w1ddkh12krj1tkxn0, suite green at every commit, GOTEST_LONG never set.

LANDED
  phase 0   13a35dd 24e9416   harness: readat helpers, the bound's counters, the corpus ledger
  phase 1   aca02c8           presence, Go side -- and the defect was worse than described (zero events, arrival included)
  phase 2a  2c1d653           node cost: 1,138 -> 1,074 MB on the staging index; an empty node ~4 KB -> <200 B
  phase 2   12a92b8           element identity: keyed arrays stored as objects of names, identity enforced at the write, three spellings canonicalized at the session, preconditions lowered
  phase 3a  8dab04f 5b8aa47   the read side of prerequisite 1: Read / Deltas / Collect / Rooted, the nine entry points deleted, the signature rule as a test, the bound observed on a staging-shaped store
  bugs      c7eb29f           !field renamed IN PLACE, so lowering diffed nothing and stored the relative op; fixed in mergeop, pinned in logd
            64fb664           both logs damaged at once (interrupted snapshot + torn tail) lost every record behind the damage on the next restart; the walk now resyncs and only the bad bytes' segments are dropped

NOT LANDED, in the order the plan gives them
  3b  the write side: per-path lowering, the stepped head removed, Require as a bounded read. The head remains an internal cache with no entry point; verifyApplies still diffs whole documents.
  4   one delta shape: the notification from the stored entry; !logd-patch-root deleted. Note that phase 3a already measured that marker's read-side cost -- it made the root non-spine on every commit.
  5   the wire: presence as a field, protocol 2, libctl and verse in one step; stream-encoded responses.
  6   index residency: the inversion and the seam.
  7   snapshots into compaction.

FINDINGS THE DOCUMENTS SHOULD ABSORB (each is in a commit message and, where it changed a design, in the document)
  - passesThrough read the patch-root marker as an operator: the root was a write on every commit, and a read at any path collected every entry through the root's copy. Spine's selectivity was not reaching reads below the root until 8dab04f.
  - "composed into one running delta" has no primitive; Read holds the writes to kp since the snapshot, each cut down to kp, which is the admitted term. rebuild_plan.md says so.
  - the stored object carries no tag (a tag would cost every element write its spine); a raised STATE is op-free and only a DELTA carries !key; kpath strips a (key) segment's quotes, so items("42") is items(42). element_identity.md amended for all three.
  - every read consults the schema to raise; a resolver that itself reads re-enters.

DECISIONS TAKEN, all as recommended in the plan: null key refused at the write; pull-over-push as a goroutine per cursor and a 64-event channel; the server's response under a configured budget (storage.readBudget, default 64 MiB), stream encoding deferred to 5; Collect's budget in bytes; identity declared before an array is written and never lost or changed.

OPEN, deliberately: the (key) sugar cannot spell a quoted value; composite identities raise untagged; the two-item allowlist (LookupRangeAll, AllSegments) is phase 7's to close.
