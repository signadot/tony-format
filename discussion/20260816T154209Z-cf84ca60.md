<!-- 2026-08-16T17:42:09+02:00 -->

Fixed in 6679423. api.ValidateForStorage is asked of a scoped write's data in NewPatcher, and
of a baseline write's not at all -- the two halves want different things:

    baseline   deterministic replay   verify it applies, once   (8e1b334)
    scope      base moves             hold it to the vocabulary

Asked of the CLIENT's patch data, before MergePatches, so the !arraydiff logd manufactures for
a positional write is not mistaken for something the client said: a scoped `votes[1] !insert`
still works, and is tested.

One existing test changed sides. TestScope_RelativeOpReevaluates demonstrated that a relative
op re-evaluates as baseline moves -- by storing one -- which was the premise
5hmq80f3h12krh1mbsn0 rests on: "leaf writes are absolute (latest-per-path is sound)... a
relative leaf op would need more, but those are not believed to exist here". That premise is
now ENFORCED rather than believed, which is a stronger footing for the overlay compaction than
it had: there is no relative op in a scope for an absolute materialization to stand in for. The
test asserts that instead, and a companion checks the same op is still storable in baseline.
