<!-- 2026-08-16T12:28:01+02:00 -->

Correction: two spans in "Why it costs something" were eaten by my shell before the issue was written (backticks in a double-quoted argument). The paragraph should read: verse charters are prose-dense — a rule, then a `---`, then a paragraph explaining the rule after it. A section of design notes standing between two rules is a natural thing to write, and it makes the WHOLE file unreadable to `verse trigger add`, not just that section. One file in that repo (the merge-gate half of docs/working/comment-drift.tony) could never be installed from its own bytes for this reason, and nobody noticed because the rules had been installed one at a time. Nothing else in the report is affected.
