<!-- 2026-09-12T00:08:51+02:00 -->

In go-tony/v0.0.212: a refused schema is schema_refused, a keying conflict is invalid_diff. Still open: a transaction short of a participant times out with a plain error (tx/coord.go:275, 'transaction timeout: not all participants joined') and handlePatch answers it storage_error; the timeout code is sent only for the participant's own timeout. The 5m default (51405301) changes when it happens, not what it is called.
