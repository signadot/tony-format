<!-- 2026-09-13T11:54:02+02:00 -->

item 9: reclassified -- naming the input as -o is a misuse, not a tier-1 defect, and 9721fe80 (temp file + rename) is dropped from sweep-core as the wrong shape (hidden intermediary, replaces links, stray file on crash). 5cbe39a8 on sweep-core refuses the misuse as a usage error before the output is opened, leaving the file as it was.
