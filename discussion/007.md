<!-- Comment 007 - 2026-07-28T08:35:48+02:00 -->

The COW fix (`3c3227c` and the `logd-cow` work described in comment 006) is merged, and this
issue's exact repro is now a regression test.

`TestScope_COW_ScopeWinsOverLaterBaseline` (`system/logd/storage/scope_cow_test.go:79`)
encodes it directly — scope writes `a.x=5`, baseline then writes `a.x=99`, and the scoped
read must still see 5 — with the comment "(The original COW isolation bug ...)". The whole
family passes on main at `911d959`:

```
--- PASS: TestScope_COW_ScopeWinsOverLaterBaseline
--- PASS: TestScope_COW_TracksOngoingBaseline
--- PASS: TestScope_COW_AncestorMerge
--- PASS: TestScope_COW_AncestorClobber
--- PASS: TestScope_COW_KeyDurability
--- PASS: TestScope_COW_PatchesSurviveCompaction
--- PASS: TestScope_COWSemantics
```

The remaining item named in comment 006 — bounded scope-overlay compaction — is tracked
separately in `5hmq80f3h12krh1mbsn0` and stays open.
