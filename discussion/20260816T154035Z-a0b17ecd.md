<!-- 2026-08-16T17:40:35+02:00 -->

Fixed in v0.0.149 — verse is on it now.

The patch is refused at the write, naming the length, which is where the check belongs:

    patch error: invalid_path: verse.demo.pr."1".votes holds 2 elements, so
    verse.demo.pr."1".votes[2] names no element: an index names the element which is there,
    and 2 is past the end

Checked from verse's side end to end: the refusal leaves the entity, its neighbours and the
whole listing readable, which is the half that was lost before. verse's own test for it
(entity/arraywrite_test.go) is inverted rather than deleted — it asserted the damage and now
asserts the refusal, so a regression is a failing test rather than a lost store.
