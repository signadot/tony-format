<!-- 2026-09-08T09:31:17+02:00 -->

-m PHASE 4 LANDED: b582efc. One delta shape, prerequisites 1 to 3 -- and the marker removed, per Scott: a marker stored on purpose and stripped on the way out is a difference between stored and delivered bytes that is possibly wrong and invisibly so, and the argument for keeping it was only the cost of taking it out.

The notification is built after lowering from the entry the log keeps, as a deep copy (deliverable); Deltas hands out the same copy of the same entry; both are raised by the one function. Live and replay are the same bytes by construction. TestDeltaIdentity asserts it at the commit -- IDENTITY against the replay and against the raised stored entry, ABSOLUTENESS as the fold from C-1 landing where the read at C lands, comments counted -- over a lowered !insert, a comment as a wrapper, a write that changes nothing, a scope's claim and two deletes, with lowering as shipped and forced.

!logd-patch-root is gone entirely: tx/patch_root.go, TagPatchRoots and every strip, DeliverablePatch, markDeltaRoots. Where an entry is applied from is read from its SHAPE (walkAndCollectPatchRoots): an operation is about the node it is on; a leaf, an array, or an empty container is a write at its path; a commented node is a statement at the comment's path; a plain object with fields is passed through. That is the reading the index (PatchChildren) and the lowering (LowerSites) already make, so the three agree because they are one rule. The cost of taking it out was smaller than I had argued: the read path had re-rooted the marker onto every projection and stripped it at every fold, and all of that is deletion.

FOUND on the way: the processor's tests had rooted entries by tag where no write produces a root -- an element inside a bare array -- and expected positional patching there. Rooted by shape, an array is applied as a unit at its path, which is what the fold (api.NextState) does with it; the tests' reading was the marker's, not the store's. The soak lower_claim_diff_test recorded (LOGD_SEEDS=500 LOGD_LOWERING=all, two breaks in 6121 that reduced to a lowered delta leaving the marker on the document root) now passes clean, and the paragraph is gone with the marker.

NOT IN THIS PHASE: item 4 of one_delta_shape.md, one rooting rule. A delta is still delivered rooted at the document; rooting it at the watched path is the projection Read already uses, and it lands with the wire in phase 5, where a watch's events are defined. The two older processor design documents carry a note pointing at the rule as built rather than a rewrite -- say if you want them rewritten.

Suite: storage ~87s, unchanged from 3b. Full logd green.

Remaining: 5 (the wire; needs the coordinated release), 7 then 6.
