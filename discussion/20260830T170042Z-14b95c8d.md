<!-- 2026-08-30T19:00:42+02:00 -->

-m Done in 2388492, with the differential argument first in a5cc604.

**The blocker dissolved on measurement.** TestLoweringScopeDifferential ran a
store with lowering off against one with it on, and the ON arm lowered NOTHING
over the stream: 0 fired against 750 skipped, because the generator writes
objects and !delete and almost nothing anyone writes is relative. Two stores
doing the same thing agreed. Its real coverage appeared only under
LOGD_LOWERING=all, where the amplifier forced the ON arm through the path 372
times.

So the property was never carried by the unlowered arm. The arms are now
LowerEverything(false) against (true), which puts the same 372 lowered writes
under the comparison with no environment set, and the count no longer moves with
LOGD_LOWERING. That is the form that survives the deletion, and it is strictly
better covered than before.

**The refusal was deader than this issue supposed.** checkStorableInScope sat
behind `!co.commitOps.LowersScopeWrites()`, and that returned s.lowering -- true
by default. So a scoped relative write was already accepted and lowered. Verified
against the pre-removal tree: the scoped !replace is accepted there, and refused
only once EnableLowering(false) is set.

That correction reaches a test. lower_test.go's
TestLowering_ScopedRelativeWriteSurvivesAMovedBaseline wrote its scope value as
DATA, on the stated grounds that 'the refusal still stands in front of the
!replace spelling'. It did not. The test now writes the !replace -- the scenario
3xn08cb6h12kr4psg5n0 described -- and also asserts baseline is undisturbed.

**One thing the issue's list missed.** lower_matrix_test.go's client row used
EnableLowering(false) to stop the engine lowering a client-lowered delta twice.
Dropping that to the default would have left LOGD_LOWERING=all double-lowering
it. Both that row and the former 'plain' row now say LowerEverything(false)
explicitly; otherwise the amplifier would collapse 'plain' into the engine row
and the matrix would compare a thing to itself.

Suite green in both modes.
