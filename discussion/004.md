<!-- Comment 004 - 2026-07-20T05:23:09+02:00 -->

Step 2 progress on branch issue-rkrvsf1c: (4bd6d73) mountCoord reader-writer coordinator — writer priority + force_after, 5 -race tests. (7ce13a1) wired into the live server: every client watch registers as a coordinator reader; mount handshake takes a coordinator write before Register (blocks new overlapping watches, drains existing, force-ends stragglers after Spec.MountForceAfter default 5s); crash still tombstones without blocking; logd writes serialized. e2e: MountBlocksOnOverlappingWatch + MountForcesOverlappingWatch. Graceful-unmount decision: bootstrap A (controller control-frame) with the core factored so an operator trigger (C) can reuse it. Remaining: per-mount force_after wire field; graceful unmount (A); explicit membership_changed + LogdSession auto-re-watch (across crash AND force); step 3 Watch composition (ancestor watches still single-route to logd, naive).
