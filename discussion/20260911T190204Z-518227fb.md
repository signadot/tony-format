<!-- 2026-09-11T21:02:04+02:00 -->

Phase 1 landed: SetSchema is one commit; schema history in the manifest and rebuilt from the log; snapshots record the schema in force; Start/Complete/Abort, usePending, the migration request and its four codes are gone; schema_refused; config bootstraps an empty store and a mismatch refuses to serve. Losing an identity needs force and leaves the elements under their names -- phase 2 rewrites. Open: raise per SchemaAt (only matters once phase 2 rewrites exist), canonicalization under the lock, scopes.
