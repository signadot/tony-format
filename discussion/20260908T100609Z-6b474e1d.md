<!-- 2026-09-08T12:06:09+02:00 -->

-m PHASE 5 LANDED, go-tony side: de41b0f (and 2a7f554, a transaction-store deadlock the parallel suite exposed, and 5514b04, the website). Session protocol 2.

PRESENCE ON THE WIRE: WatchEvent.Absent says the path holds nothing after the event -- the first event of a watch that asked to wait, and a delta that removed the path, still delivered so applying it is how the client's copy comes to hold nothing. A match keeps saying absence by not_found with its three kinds; decision 4's MatchResult field is not taken, since that would be two answers to one question.

ONE ROOTING (rg5nd1psh12 answered): every watch event is rooted at the watched path. A baseline watch projects the stored delta with the read's own projection, api.ProjectDelta -- moved into api so storage, the server and docd share the one function -- and steps only the value at its path; the whole-document stepper is gone. Blocked projections (an operator above the path) read at the path and diff. docd lifts a mount's delta to the document to trim it and projects it back.

THE RESPONSE: a plain match is encoded from the store's event stream into the frame, no node built, but OFF the loop into bytes under the session's budget -- refused past it as a node was -- not on the writer as it is read: that would hold the connection for the fold and queue every write behind a read, which is 7qayp3ha's ordering, and TestSlowReadDoesNotBlockAWrite holds it. The budget now measures the frame.

FOUND: libctl's suite had not compiled since 3b and had not run since; once it did, the canonical path spelling was found dropping a wildcard's separator (users/1.* -> users/1*), fixed. And the multi-participant session test deadlocked under the parallel suite: the tx store and a coordinator took their locks in opposite orders (2a7f554).

VERSE'S SIDE, yours: the four files move to protocol 2 -- watch deltas rooted at the watched path (nothing to extract any more), Absent on events, protocol: 2 in the hello -- with the go-tony release and go.mod bump in one step, and compaction considered with it. The bar on staging is measured after that.

The website (front page, logd and docd index pages, session and composition pages) is 5514b04, for your pass.
