<!-- 2026-09-08T15:03:09+02:00 -->

-m CONFIGURED-LINE NIT CLOSED: 1c40695, from the same verse agent. "configured compaction" logged the file config's fields, so a config naming only a cutoff printed slotsPerTier=0 while 8 was in force -- the number in the log was not the number running.

Every configured-X line now asks the store what it took: compaction logs the resolved policy whole (five fields), path snapshots the resolved tail and budget -- same defect, mine, from phase 7 -- and the write budget and index ceiling ask the store too, though their >0 guards had kept them accidentally honest. Three small accessors (CompactionConfig, PathSnapshotPolicy, IndexCeiling) beside the WriteBudget one that existed. Pinned by a test that names one field per section and holds that the log carries the defaults and never a zero the file left behind; the compaction page shows the resolved line. Full system suite green.
