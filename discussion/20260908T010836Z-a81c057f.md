<!-- 2026-09-08T03:08:36+02:00 -->

-m THE PLAN: go-tony/system/logd/storage/docs/rebuild_plan.md (9440bb9). Eight phases, each with what it reads, builds, deletes, is held by, and is done when.

Order: 0 harness, 1 presence (Go side), 2 identity, 3 read/write cutover, 4 one delta shape, 5 wire + protocol 2, 6 residency, 7 snapshots into compaction. Fixed by dependencies, not preference: a cursor seeks to a NAME so 2 precedes 3; with no stepped head there is nothing to diff against, so per-path lowering lands INSIDE 3; that is sound only once identity removes !key from patchMayAffect; and names multiply nodes, so the 4 KB empty-node fix is 2a, not part of 6.

Release points after 2, 3+4, 5, 6, 7. The staging bar is first measurable after 3+4; protocol 2 lands at 5 with libctl and verse's four files in one step.

Two survey findings the docs did not have. one_delta_shape.md cites scopeHasKeyedPaths / readScopedStateAtOverlay -- neither exists; the fallback is scope_keyed.go (patchHasUndeclaredKey, keyedArrayPaths, annotateKeyed) from lower.go:98-99 and 234-235. Argument stands, names do not; phase 0 fixes it. And patches.ApplyPatches is push-shaped where Cursor.Next pulls -- the one place the interface meets the substrate at an angle; adapter is decision 2, no substrate change.

Eight open decisions, each with a recommendation, to be raised here before built: null key in the object form (refuse at write); pull over push (goroutine + bounded channel); the server's response (Collect under a configured budget, then stream-encode at 5); presence on the wire (a field); the lowering budget (one configured number, refused past); the durable index form (append-only regions + resident directory, rebuildable from the log); Collect's unit (bytes); compaction's own working set (decide in the file).

Assumed and confirmed: verse declares no keyed arrays; staging may be wiped. Stated in the plan where a second consumer would revoke it.
