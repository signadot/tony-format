<!-- 2026-09-08T14:54:36+02:00 -->

-m PHASE 5 GAP CLOSED: 5d5ddfa. A verse agent observed that docd was exempt from its own handshake -- docd-txpool, docd-read, docd-watch, docd-watermark (and docd-base, docd-tx) built their hello by hand with no protocol, so logd assumed the current one and the docd-to-logd hop was the one hop protocol 2 could not check. One constructor now builds every hello docd sends, with the version; the test composes a read, a split write, a watch and a replay over a mount and holds that logd saw a versioned hello from each of docd's session kinds and never logged that it assumed one.
