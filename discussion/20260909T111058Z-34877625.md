<!-- 2026-09-09T13:10:58+02:00 -->

-m Fixed on main in e301ef6: the fold replaces the array at its open, wearing the array's own tag, and drops the array's events through its close. TestAnObjectWriteBeneathAnArrayInTheBaseReplacesIt holds the processor to tony.Patch on a table of eight; TestAFieldWrittenUnderAnArrayAfterASnapshotReplacesIt holds a store with the array in a snapshot to one without, byte for byte. The write-side refusal was not built: with the fold answering what the reference answers, there is nothing left to refuse.
