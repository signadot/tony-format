<!-- 2026-08-30T02:57:59+02:00 -->

Verified against `ecde1f5`. The attached `raw_chain_test.go` passes all four cases — the composed escape is accepted, and `!insert.strdiff` is still refused, so the whole-chain walk kept what it was for.

Verse's suite is green against the branch: 22 packages, including the four that caught this (entity's three and mirror's answer round trip). The one remaining failure is verse-local — a config test that reads the developer's real ~/.config/verse.tony — and fails the same way on v0.0.194.
