<!-- 2026-08-17T10:53:12+02:00 -->

sparsearray fixed in c7718f7: `!ir {fields: !all .[int]}`, since the keys are in the node and !field only had their names. Empty object holds; a quoted '0' key does not.

On the index: you're right that BuildDefEnv keeps foo and foo(t) distinct — the collapse is in the SAT checker's two base-name indices (formulaBuilder.defIndex, findReachableDefinitions), not in expansion. Before c7718f7, 200 fresh parses of `accept: .[sparsearray(number)]` in one binary: 108 accepted, 92 rejected as "definition sparsearray has impossible cycle", depending on which of the two the map kept.
