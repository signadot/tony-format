<!-- 2026-08-29T20:12:13+02:00 -->

Same as qth3kqe9h12ksxz9j9n0: this reproduced only through the scope overlay, and Storage.scopeOverlay now defaults to OFF, so it is off the default path rather than fixed. The root comment survives a scoped read when the scope's patches are replayed; it was the overlay's derivation that dropped it. EnableScopeOverlay(true) still reaches it.
