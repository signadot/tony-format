# remote build

o build uses the dirbuild pkg and it would be great if folks could publish their builds and it could be called on a URL.

To facilitate this, of course we'd need to figure out what merge ops are allowed and how to navigate relative source files.