# argo cd integration

I do not understand why ArgoCD does not provide a built-in way of
deploying a list of k8s manifests but it doesn't, apparently.

ArgoCD needs an integration with 'o build'

Plugin?
ConfigMapPlugin?