# logd: expose schema to clients

logd should expose schema to clients

schema migration/evolution needs work as well at least
at the wire protocol level

when this happens, it should also allow associating special
schema with scopes