# cli functionality is mix of ad-hoc and structured

The various cli functionality is all over the place
LSP is separate from codegen is separate from `o` and
git-issue has a different nature altogether.

This issue is to discuss the CLI organisation and
then implement whatever organisation is decided.