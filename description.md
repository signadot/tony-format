# git issue identifer conflicts

git issue is decentralised and local by default.

However, git issue uses monotonic numbered identifiers.

Obviously, this creates conflicts in distributed usage.

This issue is to discuss what to do about this.